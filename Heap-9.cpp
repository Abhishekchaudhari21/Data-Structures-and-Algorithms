#include<iostream>
using namespace std;

int count = 0, n; // Global variable to count the number of nodes in the heap tree
int key[20];
// Class to represent Heap tree 
class HeapTree {
public:
    int data; // Data of the node

    void accept() { // Function to accept data for the node
        cout << "Enter number of values: ";
        cin >> n; // Accept the number of values
    }

    void BuildHeap(int key[], int n, int ){
        
    }
};